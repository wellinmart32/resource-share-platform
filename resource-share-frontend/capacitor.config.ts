import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'resource-share-frontend',
  webDir: 'www'
};

export default config;
